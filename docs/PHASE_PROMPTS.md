# PHASE_PROMPTS.md — PROCLUSTER (адаптировано под MiMoCode)

> Загрузочные промты под каждую фазу. Файл для тебя (человека), копировать в новую сессию.
> Режим MiMoCode переключается клавишей Tab. Модель (для экономии на простом)
> настраивается отдельно через провайдера в .mimocode/mimocode.json.
> MEMORY.md MiMoCode подтягивает автоматически; в промтах продублировано для надёжности.

========================================================================
ФАЗА 0 — СКЕЛЕТ РЕПОЗИТОРИЯ
Режим: build
========================================================================
Прочитай MEMORY.md и docs/ARCHITECTURE.md (раздел структуры). Кратко перескажи план, дождись "ок".
Задача:
- Структура монорепо: backend/, frontend/, deploy/, docs/, scripts/, .githooks/, .github/workflows/.
- .gitignore (Go, Node, .env, sqlite, data-папки). .env.example со всеми переменными (без значений).
- README.md: описание + запуск + строка про `git config core.hooksPath .githooks`
  + раздел "окружение разработчика" (Go, Node, gopls, typescript-language-server).
- go.mod + cmd/procluster/main.go (только старт + лог "procluster up").
- frontend: Vite + React + TypeScript (strict), пустой каркас. Инициализируй git.
НЕ пиши бизнес-логику. В конце обнови docs/PROGRESS.md и предложи коммит.

========================================================================
ФАЗА 1 — ИНТЕГРАЦИЯ ДИЗАЙН-РЕПОЗИТОРИЯ
Режим: plan (разведка ./design-src) → build (перенос)
========================================================================
Прочитай MEMORY.md и docs/ARCHITECTURE.md. Дизайн склонирован в ./design-src
(ничего не клонируй сам, токены не ищи). Кратко перескажи план, дождись "ок".
Задача:
- Сначала в режиме plan: пройди ./design-src и верни СПИСОК файлов "оставить/удалить" с обоснованием.
  Оставляем: верстку, дизайн, UI-компоненты, модалки, стили, шрифты, layout, места размещения функций.
  Удаляем прочее (старый движок графика, лишнюю логику).
- В build: перенеси нужное в frontend/src/components и frontend/src/features.
- Переключатель темы dark/light (liquid glass) и языка RU/EN (заготовка i18n).
- Переключатель палитры свечей: (красно-зелёная)/(бело-серая).
- Спорный момент по расположению функций — СПРОСИ. В конце обнови PROGRESS.md и DECISIONS.md.

========================================================================
ФАЗА 2 — СЛОЙ ДАННЫХ: CLICKHOUSE + СЖАТИЕ  🔒
Режим: compose
========================================================================
Прочитай MEMORY.md и docs/DATA_MODEL.md ПОЛНОСТЬЮ + последние записи PROGRESS.md.
Кратко перескажи правила сжатия и округления, дождись "ок".
Задача (backend/internal/store, backend/migrations):
- Миграции ClickHouse: clusters_futures (TTL 1г), clusters_spot (TTL 3г), clusters_*_dom (по DOM_SPEC).
- ЕДИНЫЙ модуль агрегации/сжатия: TRUNCATE до 1 знака; маппинг цены в ряд по base-сжатию
  (из конфигурации, НЕ хардкод); уровни base*k до 10.
- Интерпретация трейда: isBuyerMaker → сторона; сортировка по tradeId.
- Repository-интерфейс к store. Юнит-тесты на округление/сжатие с примерами из доки.
Безопасность: запросы параметризованные. В конце обнови PROGRESS.md и DECISIONS.md
(путь агрегации старших ТФ: rollup vs из сырья).

========================================================================
ФАЗА 3 — INGEST + АГРЕГАТОР REALTIME  🔒
Режим: plan (изучить Binance WS) → compose (реализация)
========================================================================
Прочитай MEMORY.md, docs/ARCHITECTURE.md (потоки) и docs/DATA_MODEL.md.
Кратко перескажи план потоков, дождись "ок".
Задача:
- Сначала в plan: изучи Binance trades stream и REST (futures+spot), верни конспект на 1 страницу:
  формат сообщений, поля, лимиты, дозапрос пропущенных tradeId, reconnect.
- ingest: подключение к trades stream (BTCUSDT futures+spot), reconnect, защита от потери пакетов.
- aggregator: черновая агрегация текущей свечи в Redis на каждом трейде; запись в ClickHouse
  при закрытии интервала ТФ (через модуль из Фазы 2). Кэш последних 700 свечей в Redis.
- Тесты на агрегацию по ТФ. Без блокирующего I/O в горячем цикле (батчить). Обнови PROGRESS.md.

========================================================================
ФАЗА 4 — REST API + WS HUB + ЛИМИТ СЕССИЙ  🔒
Режим: build (REST) / compose (ws-hub, лимит сессий, тесты на гонки)
========================================================================
Прочитай MEMORY.md, docs/ARCHITECTURE.md и docs/AUTH_PLANS.md
(раздел "Ограничение одновременных активных графических сессий"). Кратко перескажи, дождись "ок".
Задача:
- REST: GET истории свечей с ОБЯЗАТЕЛЬНОЙ пагинацией и лимитом глубины; отдача 700 свечей из кэша.
- WS hub: подписка по (symbol, market, timeframe), рассылка live каждые 100-500мс, heartbeat,
  лимиты соединений/подписок на IP.
- Лимит графических сессий по userId (Redis Sorted Set): атомарная проверка+регистрация (Lua),
  удаление протухших по heartbeat, last-wins, сообщения session_active/evicted/rejected
  (контракт в AUTH_PLANS). Лимит per тариф из конфигурации. userId пока заглушка (Фаза 9).
- Тесты: пагинация, лимиты WS, ГОНКА на лимит сессий (test -race). Обнови PROGRESS.md.

========================================================================
ФАЗА 5 — ДВИЖОК: КАРКАС + ЯПОНСКИЕ СВЕЧИ + АКТИВНОСТЬ ОКНА
Режим: plan (изучить PixiJS) → compose (реализация)
========================================================================
Прочитай MEMORY.md и docs/CHART_ENGINE.md ПОЛНОСТЬЮ + раздел про сессии в AUTH_PLANS.md.
Кратко перескажи архитектуру и требования к 60 FPS, дождись "ок".
Задача (frontend/src/chart-engine, ИЗОЛИРОВАННО):
- При необходимости в plan: конспект по PixiJS для high-perf чартов (ParticleContainer/Mesh,
  батчинг, типизированные геометрии, синхронизация WebGL+Canvas2D).
- Модули: Renderer, Viewport (zoom к курсору, пан, инерция), Scales (оси), DataStore (тип. массивы).
- Режим: японские свечи, 60 FPS. Управление: drag; колесо=zoom к курсору; SHIFT+колесо=верт;
  CTRL+колесо=гор; drag по осям=зум по оси. Контракт engine.* включая pause()/resume().
- Активность окна: leader-election между вкладками (BroadcastChannel) + обработка WS-сообщений
  session_active→resume / session_evicted|rejected→pause + плашка; heartbeat ~10с.
НЕ реализуй футпринт/кластера. Движок не импортирует UI. Обнови PROGRESS.md.

========================================================================
ФАЗА 6 — ДВИЖОК: ФУТПРИНТ / КЛАСТЕРА / ИМБАЛАНС / АВТО-РЕЖИМ
Режим: compose
========================================================================
Прочитай MEMORY.md, docs/CHART_ENGINE.md и docs/DATA_MODEL.md (имбаланс).
Кратко перескажи рендереры и пороги авто-режима, дождись "ок".
Если фаза большая — предложи разбить на 6a (рендер) и 6b (авто+сжатие+имбаланс).
Задача:
- Рендереры: clusterRenderer (bid/ask цифры, Canvas2D), footprintRenderer + гистограмма, barRenderer.
- Режимы (панель): авто|японские|бары|футпринт|кластера. АВТО: <50 свечей — кластеры, 50-200 — футпринт,
  200+ — японские свечи (пороги в конфиг). Объём в ячейке: bid×ask|volume|delta. Палитра свечей.
- Уровни сжатия (объединение рядов из base на лету). Имбаланс >300% диагональный (ask vs bid[price-1]).
- 60 FPS: object pool, батчинг, dirty-flag. Обнови PROGRESS.md.

========================================================================
ФАЗА 7 — РАБОЧИЕ ПРОСТРАНСТВА / LAYOUTS
Режим: build
========================================================================
Прочитай MEMORY.md и docs/CHART_ENGINE.md (layouts). Кратко перескажи, дождись "ок".
Задача: выбор 1/2-гориз/2-верт график; перетаскиваемый разделитель; авто-заполнение области;
растяжение при сворачивании стакана; хук под будущую синхронизацию курсора (не реализовывать).
Обнови PROGRESS.md.

========================================================================
ФАЗА 8 — СТАКАН DOM + FEAR&GREED + СНАПШОТЫ  🔒
Режим: compose (backend depth-sync/snapshotter) / build (UI)
========================================================================
Прочитай MEMORY.md и docs/DOM_SPEC.md ПОЛНОСТЬЮ. Кратко перескажи правила снапшотов и depth-sync, дождись "ок".
Backend: depth stream с корректной синхронизацией (lastUpdateId, снапшот+diff); dom-snapshotter
(futures 1м/spot 15м, момент закрытия интервала, агрегация по base-сжатию, запись в clusters_*_dom,
truncate до 1 знака); fng-fetcher (раз в день, кэш).
Frontend: панель справа — F&G сверху, стакан снизу + гистограмма; уровни=base-сжатие; ±5%;
обновление 1/сек; прокрутка + авто-центрирование после 1с; кнопка свернуть/развернуть.
Безопасность: БД только во внутренней docker-сети. Обнови PROGRESS.md.

========================================================================
ФАЗА 9 — АВТОРИЗАЦИЯ И ПОЛЬЗОВАТЕЛИ  🔒
Режим: compose
========================================================================
Прочитай MEMORY.md, docs/AUTH_PLANS.md и docs/SECURITY.md ПОЛНОСТЬЮ. Кратко перескажи, дождись "ок".
Задача:
- auth + SQLite: email (argon2id), Google OAuth (state, id_token), роли Free/Pro/VIP/Admin.
- Сессии: refresh в httpOnly+Secure+SameSite cookie, короткий access, ротация.
- Rate-limit login/register/recovery (Redis), lockout. ОБЯЗАТЕЛЬНОЕ подтверждение email.
  Мягкий rate-limit регистраций по IP.
- Настройки пользователя (SQLite + localStorage для гостя). Гейтинг истории по тарифу на бэкенде.
- Связать лимит графических сессий (из Фазы 4) с реальным userId+тарифом (Free=1, Pro/VIP по конфигу,
  Admin без лимита). Frontend: модалки логина/регистрации из дизайна.
- Тесты на authZ, rate-limit, связку userId↔лимит сессий. Обнови PROGRESS.md и DECISIONS.md.

========================================================================
ФАЗА 10 — ПРОФИЛЬ + ТАРИФЫ
Режим: build
========================================================================
Прочитай MEMORY.md и docs/AUTH_PLANS.md (профиль, тарифы). Кратко перескажи, дождись "ок".
Задача: профиль (ник, аватар из набора, email, дата рег., поле ТГ-токена, смена пароля);
инфо о подписке (уровень, дата оплаты/истечения, осталось дней, статус); блок тарифов со
сравнением Free/Pro/VIP + кнопка активации. Лимиты из конфигурации, НЕ хардкод. Обнови PROGRESS.md.

========================================================================
ФАЗА 11 — ИНДИКАТОРЫ + CLUSTER SEARCH
Режим: compose
========================================================================
Прочитай MEMORY.md и docs/INDICATORS.md ПОЛНОСТЬЮ. Кратко перескажи логику Cluster Search и права, дождись "ок".
Задача: система индикаторов (интерфейс, оверлей/панель, пресеты SQLite+Redis, набор по умолчанию из
админки per символ/рынок/ТФ); Cluster Search ТОЧНО по формулам доки (средний/крупный фильтр,
объединение уровней по умолч.3, перевес max(bid%,ask%)>порог, дельта, расположение, визуал per фильтр,
одно выделение в баре); права Free/Pro/VIP. Юнит-тест на пример (180/3/60% → bid, total=200). Обнови PROGRESS.md.

========================================================================
ФАЗА 12 — АДМИНКА  🔒
Режим: compose (history-loader) / build (UI)
========================================================================
Прочитай MEMORY.md, docs/ADMIN.md и docs/SECURITY.md (админка). Кратко перескажи, дождись "ок".
Backend: history-loader с data.binance.vision (символ+рынок+диапазон, прогресс, ИДЕМПОТЕНТНОСТЬ,
фоновая задача, тот же модуль сжатия); добавление тикеров (priceTick, base-сжатие, TTL, сжатие per ТФ);
мониторинг (CPU/RAM/диск/размер БД/логи); статистика юзеров; настройка лимитов тарифов (вкл. лимит
сессий); индикаторы по умолчанию; audit log. UI: админ-раздел из дизайна, прогресс-бар.
Безопасность: проверка роли Admin на КАЖДОМ эндпоинте + middleware. Обнови PROGRESS.md.

========================================================================
ФАЗА 13 — ЛЕНДИНГ procluster.online
Режим: build
========================================================================
Прочитай MEMORY.md и контекст дизайна. Кратко перескажи план лендинга, дождись "ок".
Задача: презентационный сайт (блоки возможностей, RU/EN, dark/light, liquid glass, дизайн-компоненты,
CTA на регистрацию/переход в chart.procluster.online). Обнови PROGRESS.md.

========================================================================
ФАЗА 14 — ДЕПЛОЙ + CI/CD  🔒
Режим: build
========================================================================
Прочитай MEMORY.md, docs/ARCHITECTURE.md (топология) и docs/SECURITY.md (VPS). Кратко перескажи, дождись "ок".
Задача: docker-compose (go-app, clickhouse, redis, caddy; БД только во внутренней сети);
Caddyfile (домены, авто-TLS, security-заголовки, rate-limit, лимит размера); GitHub Actions
(build+test -race+lint+автодеплой при push в main; gitleaks первым job, deploy через needs;
.env через секреты CI); инструкция защиты VPS в README (SSH по ключам, fail2ban, firewall,
requirepass Redis, не-default ClickHouse, бэкапы). Обнови PROGRESS.md.

========================================================================
ФАЗА 15 — АУДИТ БЕЗОПАСНОСТИ  🔒
Режим: plan (read-only; исправления потом отдельно в build/compose)
========================================================================
Прочитай MEMORY.md и docs/SECURITY.md (весь чек-лист). Это ПРОВЕРКА и ЗАЩИТА, не создание атак.
Кратко перескажи план, дождись "ок".
Проверки (для каждой: найдено / риск низкий-средний-высокий / рекомендация):
1) Секреты в коде И в истории git. 2) Валидация ввода и параметризация SQL.
3) AuthN/AuthZ на приватных/админ-маршрутах. 4) Rate-limit (login/register/recovery/admin) и лимиты пагинации/WS.
5) Уязвимые зависимости (go list -m all, npm audit). 6) CORS/CSP/security-заголовки/лимиты WS.
7) Лимит графических сессий: на бэкенде по userId; атомарность (Lua); чистка протухших по heartbeat;
   last-wins корректен; нельзя получать данные без heartbeat; лимит per тариф из конфигурации;
   гость отдельно.
Собери сводный отчёт, приоритизируй, заведи задачи на исправление. Обнови PROGRESS.md и DECISIONS.md.

========================================================================
ФАЗА 16+ — ОПТИМИЗАЦИЯ / НОВЫЕ ТИКЕРЫ / HEATMAP
Режим: plan (анализ/профайлинг) → build/compose (правки)
========================================================================
Прочитай MEMORY.md и относящийся спек. Кратко перескажи, дождись "ок".
Примеры: MessagePack при доказанном bottleneck (DECISIONS.md); новые тикеры через админку (сжатие
не хардкод); тепловая карта плотностей по истории DOM-снапшотов; миграция SQLite→PostgreSQL.
Обнови PROGRESS.md.
